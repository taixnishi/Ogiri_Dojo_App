
<div class="themes-new main">
  <div class="themes-new container">
    <div class="theme-info">
      <h2>使い方</h2>
    </div>
    <div class="themes-new rule">
      <ul>
        <li>・投稿フォームにお題を記入</li>
        <li>・画像をお題にされる方は、画像選択ボタンからファイルを選択してください</li>
        <li>・お題作成のボタンを押すとお題が投稿されます</li>
      </ul>
    </div>
    <div class="theme-info">
      <h2>投稿フォーム</h2>
    </div>
    <div class="themes-new contents">
      <%= form_with model: @theme, url: { action: :create }, class: "new_list", local: true do |f| %>
        <% if @theme.errors.any? %>
          <p class="text-danger">1～255文字以内で入力してください</p>
        <% end %>
        <%= f.label :content , "お題"%>
        <%= f.text_area :content, autofocus: true, class: "form-control listName", placeholder: "1文字以上入力してください" %>
        <div class="field">
          <%= f.label :image, "画像送信" %>
          <%= f.file_field :image %>
        </div>
        <div class="text-center"><%= f.submit "お題作成", class: "submitBtn" %></div>
      <% end %>
    </div>
  </div>
</div>
